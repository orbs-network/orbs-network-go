version: '2'

services:
  orbs-tests:
    image: orbs:build
    command: bash -x test.sh
    environment:
      GIT_BRANCH: $GIT_BRANCH
      GIT_COMMIT: $GIT_COMMIT
    volumes:
      - ../../_out:/go/src/github.com/orbs-network/orbs-network-go/_out
