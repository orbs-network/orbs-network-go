FROM 727534866935.dkr.ecr.us-west-2.amazonaws.com/orbs-network-base:latest

WORKDIR /src

RUN apt-get install -y git bash libc6-dev

ADD . /src

RUN env

RUN go env

ARG SKIP_DEVTOOLS

ARG GIT_COMMIT

ARG BUILD_FLAG

ARG SEMVER

RUN ./build-binaries.sh
